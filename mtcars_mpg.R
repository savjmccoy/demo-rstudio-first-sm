#view the first few rowws of mtcars dataset
head(mtcars)
#calculate the avg mpg among all cars and save it as a variable
mean_mpg <- mean(mtcars$mpg)
#print out the mean mpg
mean_mpg

#literally just adding some comments to make sure i can push to github